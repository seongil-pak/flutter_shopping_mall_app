name: flutter_shopping_mall_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 추가된 패키지
  image_picker: ^0.8.7+3 # 이미지 선택용
  intl: ^0.18.0 # 숫자 포맷팅용 패키지

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true